# mini-port-scanner
A Python-based TCP port scanner that checks open ports on a target IP address or hostname within a user-defined range. Built using Python’s socket module to demonstrate basic network reconnaissance and cybersecurity fundamentals.
